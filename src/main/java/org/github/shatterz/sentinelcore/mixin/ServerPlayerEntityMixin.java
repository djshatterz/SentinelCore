package org.github.shatterz.sentinelcore.mixin;

/**
 * Unused placeholder — scoreboard-based prefixes supersede this mixin. Left as a no-op so we can
 * reintroduce future hooks without churn.
 */
public final class ServerPlayerEntityMixin {
  private ServerPlayerEntityMixin() {}
}
